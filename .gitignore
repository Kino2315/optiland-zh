# ─────────────────────────────────────────────────────────────────────────
# 本项目专属
# ─────────────────────────────────────────────────────────────────────────
# 提取器和审计工具的工作产物：每次都能重新生成，不该进版本库。
# 这几条是 GitHub 的 Python 通用模板里没有的。
strings.json
audit_out.txt
_all.txt
static_list.txt

# 开发期临时调试脚本（用完即删）
_review.py
_dump.py
_dump2.py
_chars.py

# ─────────────────────────────────────────────────────────────────────────
# 构建产物
# ─────────────────────────────────────────────────────────────────────────
build/
dist/
*.egg-info/
src/*.egg-info/

# ─────────────────────────────────────────────────────────────────────────
# Python 通用（取自 GitHub 官方 Python 模板）
# ─────────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
.installed.cfg
*.egg
MANIFEST

# 测试 / 覆盖率
.pytest_cache/
.tox/
.nox/
.coverage
.coverage.*
coverage.xml
*.cover
*.py,cover
htmlcov/
.hypothesis/
cover/

# 类型检查 / 格式化缓存
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
.ruff_cache/
.cython_debug/

# 打包 / 环境
.python-version
__pypackages__/
pip-log.txt
pip-delete-this-directory.txt

# ─────────────────────────────────────────────────────────────────────────
# 虚拟环境
# ─────────────────────────────────────────────────────────────────────────
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# ─────────────────────────────────────────────────────────────────────────
# 编辑器 / 系统
# ─────────────────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
.spyderproject
.spyproject
.ropeproject
.ipynb_checkpoints
.DS_Store

# ─────────────────────────────────────────────────────────────────────────
# 日志
# ─────────────────────────────────────────────────────────────────────────
*.log

# ─────────────────────────────────────────────────────────────────────────
# 刻意【不】忽略的东西（本项目是汉化项目，默认的 Python 模板会误伤）
# ─────────────────────────────────────────────────────────────────────────
# GitHub 的 Python 模板里有 *.mo 和 *.pot（gettext 编译产物）。
# 本项目不用 gettext —— 词库是 JSON，随包分发。但如果以后有人贡献
# Qt 的编译翻译文件（*.qm），那属于要发布的资产，必须提交。
# 所以这里不写 *.mo / *.pot，避免以后加翻译文件时被静默忽略。
